export interface StockParCategorie {
    categorie: string;           // Nom de la catégorie d’équipement
    quantiteStock: number;       // Quantité totale en stock
    valeurTotalStock: number;    // Valeur totale du stock (en FCFA ou autre)
}
